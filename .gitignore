work/node_modules/
# ⚠️ Root-level too. `npm install playwright` at the repo root put 176 files and 366k
# lines into a commit that only wanted two test files — work/node_modules/ alone did not
# cover it, because the install was not in work/.
/node_modules/
/package-lock.json
/package.json
work/package-lock.json
work/package.json
work/*.png
__pycache__/
*.py[cod]
work/.shots
private/ddcc-question-library.json

# Local scratch and archives — never served, never deployed. Kept out of `git status`
# so a real pending change is visible at a glance.
.wrangler/
tmp/
_to_delete/
_deferred/
work/artifacts/

# Intermediate output of work/build-vendor-dollars.mjs. Regenerate it from the source
# sheets rather than committing it: it is derived, and a stale copy in the tree is a
# board full of prices nobody can trace back to a run.
work/vendor-dollars.json
