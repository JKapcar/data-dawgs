# PPN v3 auction deploy — Claude Code handoff

Repo: the datadawgs216.com site repo (Cloudflare Pages/Worker; `build-data.js` is the data pipeline).
Everything in `deploy/` is final. **Do not recompute, smooth, cap, or merge values.** Join by NFL `id`, never by display name.

## 1. Place files
- `deploy/ppn-auction-values.json`  -> served at `/data/ppn-auction-values.json`
- `deploy/ppn-auction-method.json`  -> `/data/ppn-auction-method.json`
- `deploy/ppn-auction-method.md`    -> `/data/ppn-auction-method.md`
- `deploy/ppn-auction-board.html`   -> `/ppn-auction-board.html` (it fetches `data/ppn-auction-values.json` relative; keep paths)

If `/data/` files are generated by `build-data.js`, add these as static passthrough inputs rather than hand-copying into the output dir, so the pipeline doesn't delete them on next build.

## 2. Update machine surfaces
- `/llms.txt`: add the three `/data/ppn-*` endpoints.
- `/data/surfaces.json`: add entries — human page `ppn-auction-board.html` (status: live), machine equivalents the three endpoints (status: live).
- `dd_site_map` (Worker): add the three endpoints to the `data` array and `ppn-auction-board.html` to `pages` with the description: "PPN league auction board — Target $, conversion-sensitivity bands, ETR delta."
- Copy rule: describe the model as "reproducible and red-teamed", never "bulletproof" or "outcome-validated".

## 3. Build-blocking tests (run BEFORE deploy; any failure blocks)
From `deploy/ppn-auction-values.json`:
- players.length == 424; zero kickers; zero duplicate ids; zero negative targets
- sum(target) == 2800 exactly; 121 players with target > 0
- Gibbs 90, Bijan 85, JSN 77, Chase 77, Puka 76
- Hamilton check: floor(exact) + largest-remainder must reproduce every `target`
- All DST targets == 0; every positive target satisfies low <= target <= high
- source_snapshot_sha256 == 1d772b1db0b5e79a06835b89d0589c5cc469930910798fef86d0975c3b511b8c
- AMENDED GATE: Jayden Higgins (00-0038130) absent-or-$0. He is NOT a row (absent from the ETR
  source snapshot); the override in the method contract is recorded for audit and zeroes nothing.
  The original "must be $0" wording cannot pass; method.md carries the amendment note. Do not add a row.

## 4. Do NOT
- Do not touch legacy `pool.json` columns (`half14`, ETR fields) or the MV big board.
- Do not publish the raw ETR subscriber CSV; only the SHA-256 hash appears publicly.
- Do not resurrect the rejected adjustments (concavity, games-played haircuts, QB multipliers, $1 floor).

## 5. After deploy
- curl the three endpoints and diff against `deploy/` bytes.
- Load `/ppn-auction-board.html` desktop + ~390px mobile; confirm header meta, disclaimer line, band strips, Δ ETR colors, draft-mode strikethrough.
- Report: commit hash, deployed URLs, test output.

## Context for the agent
Board provenance: ETR half-PPR auction $ (private snapshot, hashed) converted to a 14-team, $200,
no-K, 2-flex, $0-bid league via a VOR-based conversion with 0.5/0.5 depth/scoring pass-through,
Hamilton-rounded to $2,800. low/high = conversion-assumption sensitivity, not bid ceilings.
Known open items (do not fix in this deploy): keeper inflation (Sep 8 deadline), out-of-sample validation.
